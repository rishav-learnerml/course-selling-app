import { Request, Response, Router } from "express";

const courseRouter = Router();

courseRouter.get('/all',(req:Request,res:Response)=>{
    
})

courseRouter.get('/purchased',(req:Request,res:Response)=>{

})

export default courseRouter;